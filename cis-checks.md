| CIS Control Number | Profile Level | Control Name / Scope | Detail / Validation Logic | Auditing Engine / Pass Check Command Validation |
| :--- | :--- | :--- | :--- | :--- |
| **1.1** | Level 1 | Automatic Software Updates | Audits the configuration to guarantee the system actively polls Apple's servers for outstanding patches to prevent security drift. | <ul><li>`/usr/bin/sudo /usr/sbin/softwareupdate --schedule`</li></ul> |
| **1.2** | Level 1 | Update Background Downloads | Checks that updates are aggressively pre-downloaded in the background via system daemons to reduce the time between patch release and staging. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Preferences/com.apple.SoftwareUpdate AutomaticDownload -bool`</li></ul> |
| **1.3** | Level 1 | Automated OS Installations | Verifies that OS-level patches are allowed to install autonomously or force prompt the user to prevent zero-day operating system exploitation. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Preferences/com.apple.SoftwareUpdate AutomaticallyInstallMacOSUpdates -bool`</li></ul> |
| **1.4** | Level 1 | App Store App Installations | Mandates that first-party and App Store binaries are continuously updated without requiring localized administrator credentials. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Preferences/com.apple.commerce AutoUpdate -bool`</li></ul> |
| **1.5** | Level 1 | Silent Security RSR / Data Patches | Ensures that Apple’s background Rapid Security Responses (RSR) and XProtect malware definitions are installed silently and immediately to neutralize active threats. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Preferences/com.apple.SoftwareUpdate CriticalUpdateInstall -bool`</li></ul> |
| **2.1.1.1** | Level 1 | iCloud Password & Keychain Sync | Queries keychains to ensure enterprise secrets, passwords, and tokens are restricted from syncing to unmanaged Apple IDs over public iCloud services. | <ul><li>`/usr/bin/sudo /usr/libexec/PlistBuddy -c "Print :Accounts:0:Services:KEYCHAIN_SYNC:Status" /Library/Preferences/com.apple.mobiledevice.passwordpolicy.plist`</li></ul> |
| **2.1.1.2** | Level 1 | iCloud Drive File Syncing | Checks if enterprise configuration profiles block iCloud Drive syncing to mitigate corporate data exfiltration risks into personal cloud storage. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Managed\ Preferences/com.apple.applicationaccess allowCloudDocumentSync`</li></ul> |
| **2.1.1.3** | Level 2 | iCloud Desktop & Documents | Audits whether configuration profiles actively prevent local desktop directories from being offloaded onto external, unmonitored cloud spaces. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Managed\ Preferences/com.apple.finder EnterpriseDesktopDocumentSyncDisabled`</li></ul> |
| **2.2.1** | Level 1 | Screensaver Max Timeout Lock | Validates plist properties to confirm that unattended hardware triggers an automated lock state within 20 minutes to minimize physical device hijacking windows. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Managed\ Preferences/com.apple.screensaver idleTime`</li></ul> |
| **2.2.2** | Level 1 | Screen Lock Prompt Enforcement | Checks that a password is required immediately or shortly after initiating so that an activated screensaver cannot be bypassed without credentials. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Preferences/com.apple.screensaver askForPassword`</li><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Preferences/com.apple.screensaver askForPasswordDelay`</li></ul> |
| **2.2.3** | Level 1 | Passive Bluetooth Discoverability | Checks the core Bluetooth controller properties to ensure the machine does not advertise its physical location or hostname continuously, preventing tracking. | <ul><li>`/usr/bin/sudo /usr/sbin/system_profiler SPBluetoothDataType`</li></ul> |
| **2.2.4** | Level 1 | Remote Management Protection | Checks launchdaemons to confirm that unauthorized VNC, ARD, or Screen Sharing utilities are completely neutralized unless managed through an explicit MDM exception. | <ul><li>`/usr/bin/sudo /bin/launchctl print-disabled system \| /usr/bin/grep com.apple.RemoteDesktop`</li></ul> |
| **2.3.1** | Level 1 | Local Volumes Full Disk Encryption | Uses status checks to certify that full-disk XTS-AES-256 encryption (FileVault) protects corporate data-at-rest from loss or physical theft. | <ul><li>`/usr/bin/sudo /usr/sbin/fdesetup status`</li></ul> |
| **2.3.2** | Level 1 | Institutional/Personal Key Escrow | Verifies that encryption recovery keys are actively reported back and escrowed safely within the MDM inventory rather than remaining solely in unmanaged local storage. | <ul><li>`/usr/bin/sudo /usr/sbin/fdesetup haspersonalrecoverykey`</li><li>`/usr/bin/sudo /usr/sbin/fdesetup hasinstitutionalrecoverykey`</li></ul> |
| **2.4.1** | Level 1 | Application Layer Firewall Core Status | Evaluates the state of the host-based application firewall to ensure it is operating and actively guarding open network sockets from unauthenticated ingress traffic. | <ul><li>`/usr/bin/sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate`</li></ul> |
| **2.4.2** | Level 1 | Firewall ICMP Stealth Configuration | Confirms the firewall is configured to ignore unsolicited ICMP pings and probing requests, making the endpoint invisible to routine automated network scanning. | <ul><li>`/usr/bin/sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getstealthmode`</li></ul> |
| **2.10.1.2** | Level 2 | Silicon Energy Sleep Optimization | Audits the power management settings to ensure the display and system sleep timers conform to power/lock security mandates. | <ul><li>`/usr/bin/sudo /usr/bin/pmset -g custom \| /usr/bin/grep -E "(sleep\|displaysleep)"`</li></ul> |
| **3.1** | Level 1 | Network Subnet Shared Directories | Inspects Apple File Protocol (AFP) and SMB network daemon services to enforce that endpoints do not serve as ad-hoc, unmonitored file repositories. | <ul><li>`/usr/bin/sudo /bin/launchctl print-disabled system \| /usr/bin/grep com.apple.smbd`</li></ul> |
| **3.2** | Level 1 | Legacy CUPS Network Print Pools | Disables the CUPS external network broadcast layer to prevent malicious local print job injections or discovery mapping from remote actors. | <ul><li>`/usr/bin/sudo /usr/bin/cupsctl \| /usr/bin/grep "_share_printers=0"`</li></ul> |
| **3.3** | Level 1 | Cross-Process Remote AppleEvents | Validates that the Inter-Process Communication (IPC) system for AppleEvents across network boundaries is turned off to neutralize legacy remote execution channels. | <ul><li>`/usr/bin/sudo /usr/sbin/systemsetup -getremoteappleevents`</li></ul> |
| **3.4** | Level 1 | Bridge/NAT Ad-hoc Internet Sharing | Restricts network configuration parameters to guarantee that an endpoint cannot route or bridge enterprise network traffic to unauthenticated downstream devices. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Preferences/SystemConfiguration/com.apple.nat NAT -dict`</li></ul> |
| **3.5** | Level 1 | P2P Asset Content Caching Relays | Enforces that standard endpoints do not function as localized peer-to-peer asset relays, reducing unauthorized caching of cross-user payloads. | <ul><li>`/usr/bin/sudo /usr/bin/AssetCacheManagerUtil status`</li></ul> |
| **4.1** | Level 1 | Unified Security Daemon Initialization | Queries the system launch configuration to ensure the core Unix audit log engine boots at initialization and continuously tracks low-level kernel event records. | <ul><li>`/usr/bin/sudo /bin/launchctl list com.apple.auditd`</li></ul> |
| **4.2** | Level 1 | Kernel Scope Activity Flags Configuration | Parses configuration to verify tracking for high-risk actions including execution privileges, authentication attempts, and network shifts. | <ul><li>`/usr/bin/sudo /usr/bin/grep -E "^flags:" /etc/security/audit_control`</li></ul> |
| **4.3** | Level 1 | Low Volume Storage Threshold Warning | Confirms the audit control configurations notify system monitors prior to full disk consumption, ensuring compliance logs are never dropped. | <ul><li>`/usr/bin/sudo /usr/bin/grep -E "^minfree:" /etc/security/audit_control`</li></ul> |
| **5.1.1** | Level 1 | System Integrity Protection Locks | Evaluates system configuration variables to verify that core OS file spaces, directories, and NVRAM variables are explicitly hardware-locked against tampering. | <ul><li>`/usr/bin/sudo /usr/bin/csrutil status`</li></ul> |
| **5.1.2** | Level 1 | System Mobile File Integrity Run-times | Validates that the boot argument flags do not disable the security run-time enforcement engine responsible for verifying signed application codes. | <ul><li>`/usr/bin/sudo /usr/sbin/nvram boot-args`</li></ul> |
| **5.3** | Level 1 | Local Account Minimum Length Flags | Evaluates local authentication policies and account directory settings to block creation or reuse of weak user passwords (12+ characters). | <ul><li>`/usr/bin/sudo /usr/bin/profiles -P -o stdout \| /usr/bin/grep minLength`</li></ul> |
| **5.4** | Level 1 | Interactive Attempt Lockout Windows | Imposes an upper limit on continuous incorrect password entries (typically 5 attempts) to permanently neutralize rapid-fire brute force attacks. | <ul><li>`/usr/bin/sudo /usr/bin/profiles -P -o stdout \| /usr/bin/grep maxFailedAttempts`</li></ul> |
| **5.5** | Level 1 | Sudo Terminal Transient Session Expirations | Audits the sudoers rule configurations to ensure execution privileges require valid password reassessment, eliminating long session timeouts. | <ul><li>`/usr/bin/sudo /usr/bin/sudo -V \| /usr/bin/grep "Authentication timestamp"`</li></ul> |
| **5.6** | Level 1 | Distinct Root Session Executable Logs | Establishes distinct logging parameters for sudo terminal output to record exactly what binary execution attempts occurred under elevated root context. | <ul><li>`/usr/bin/sudo /usr/bin/sudo -V \| /usr/bin/grep -E "(log_allowed\|log_denied)"`</li></ul> |
| **5.7** | Level 1 | Secure Login Window Auth Database | Validates that the screensaver system properly calls the security authorization database to enforce authentication on wake. | <ul><li>`/usr/bin/sudo /usr/bin/security authorizationdb read system.login.screensaver`</li></ul> |
| **6.1** | Level 1 | Guest Context OS Pre-Boot Access | Validates account configuration trees to remove the unauthenticated Guest logon path from the pre-boot login window. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Preferences/com.apple.loginwindow GuestEnabled`</li></ul> |
| **6.2** | Level 1 | Anonymous Network Share Guest Mounts | Reviews network share access permissions to ensure anonymous or unauthenticated guest network tokens are explicitly denied mount rights. | <ul><li>`/usr/bin/sudo /usr/bin/defaults read /Library/Preferences/com.apple.AppleFileServer guestAccess`</li></ul> |
| **6.3** | Level 1 | Account Directory Root Context Status | Verifies the directory services backend has the primary interactive Unix root user disabled, forcing administrators to use trace-accountable sudo constructs. | <ul><li>`/usr/bin/sudo /usr/bin/dscl . -read /Users/root UserShell \| /usr/bin/grep "/usr/bin/false"`</li></ul> |
| **6.4** | Level 1 | Multi-Tenant Profile Isolation Masks | Runs permission sweeps over home structures (POSIX 0700 or tighter) to prevent secondary local users from reading or altering private profile documents. | <ul><li>`/usr/bin/sudo /usr/bin/stat -f "%A %N" /Users/*`</li></ul> |